server:
  port: 8080

spring:
  datasource:
    initialization-mode: always
    platform: postgresql
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/cashbox
    username: postgres
    password: 112233

